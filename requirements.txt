# Core training (pure HF + PEFT — no Unsloth needed)
transformers>=4.56.0
datasets>=3.5.0,<4.0.0
torch>=2.5.0
trl>=0.18.0
accelerate>=1.2.0
bitsandbytes>=0.45.0
peft>=0.18.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0
torchaudio>=2.5.0
audiomentations>=0.37.0
scipy>=1.12.0

# Evaluation
jiwer>=3.0.0
whisper-normalizer>=0.1.0

# Benchmark (optional — for scripts/00_benchmark.py)
# faster-whisper>=1.0.0    # uncomment to benchmark Systran/faster-whisper
# nemo_toolkit[asr]        # uncomment to benchmark NVIDIA FastConformer
# qwen-asr                 # uncomment to benchmark Qwen3-ASR-1.7B
# qwen-omni-utils[decord]  # uncomment to benchmark Qwen2.5-Omni-7B

# Data collection
yt-dlp>=2024.0.0

# Utilities
tqdm>=4.66.0
numpy>=1.26.0
pandas>=2.1.0
